use printf;

void print(int a[], int n) {
    for (int i = 0; i < n; ++i) {
        if (i != 0)
            printf(", ");
        printf("%d", a[i]);
    }
    printf("\n");
}

int main() {
    int a[10];
    int n = 10;
    for (int i = 0; i < n; ++i) {
        a[i] = i * i;
    }
    print(a, n);
    return 0;
}

// 0, 1, 4, 9, 16, 25, 36, 49, 64, 81