use printf;
use scanf;

int fact(int n) {
    if (n <= 1)
        return 1;
    return n * fact(n - 1);
}

int main() {
    int a;
    printf("Input n: ");
    scanf("%d", &a);
    printf("%d! = %d\n", a, fact(a));
    return 0;
}